# pytest.ini — central pytest config
[pytest]

# -q = quiet mode: minimal test output (just dots) for cleaner, faster logs.
addopts = -q

# Add 'src/' to sys.path so tests can import your code without installing the package.
pythonpath = src
